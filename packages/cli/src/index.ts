#!/usr/bin/env node

import { CORE_PACKAGE_NAME } from "@repograph/core";

/**
 * Phase 1 CLI entrypoint.
 *
 * Commands are added in the scanner and graph phases.
 */
console.log(`${CORE_PACKAGE_NAME} CLI foundation`);
