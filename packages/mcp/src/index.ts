import { CORE_PACKAGE_NAME } from "@repograph/core";

/**
 * Phase 1 MCP boundary.
 *
 * MCP tools will delegate to core retrieval APIs in a later phase.
 */
export const MCP_CORE_PACKAGE = CORE_PACKAGE_NAME;
